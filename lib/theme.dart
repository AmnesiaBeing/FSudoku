import 'dart:ui';

import 'package:flutter/material.dart';

class MyTheme {
  final Color colorBackgroundCellDisabled = Colors.black38.withAlpha(80);
  final Color colorBackgroundCellEmpty = Colors.white30.withAlpha(128);
  final Color colorBackgroundCellFocus = Colors.yellow[700].withAlpha(80);
  final Color colorBackgroundCellHover = Colors.green[700].withAlpha(80);
  final Color colorBackgroundCellValue = Colors.blue[700].withAlpha(128);
  final Color colorBackgroundCellValueFixed = Colors.purple[700].withAlpha(128);
  final Color colorCandidateTextNumber = Colors.black;
  final Color colorCandidateTextNumberDisabled = Colors.black26;
  final Color colorTextNumber = Colors.white;
}

class Theme0 extends MyTheme {
  final Color colorBackgroundCellDisabled = Colors.black38.withAlpha(80);
  final Color colorBackgroundCellEmpty = Colors.white30.withAlpha(128);
  final Color colorBackgroundCellFocus = Colors.yellow[700].withAlpha(80);
  final Color colorBackgroundCellHover = Colors.green[700].withAlpha(80);
  final Color colorBackgroundCellValue = Colors.blue[700].withAlpha(128);
  final Color colorBackgroundCellValueFixed = Colors.purple[700].withAlpha(128);
  final Color colorCandidateTextNumber = Colors.black;
  final Color colorCandidateTextNumberDisabled = Colors.black26;
  final Color colorTextNumber = Colors.white;
}
