import 'package:flutter/material.dart';

// 实在不会配色，看起来像模像样就行了
final Color CellBgWithFixedNumber = Colors.grey;
final Color CellBgWithoutFixedNumber = Colors.white;
final Color CellBgFocused = Colors.blue.withAlpha(100);
final Color CellBgHovered = Colors.green.withAlpha(100);
final Color CellBgWarning = Colors.red.withAlpha(100);
